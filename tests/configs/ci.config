# CI/CD Configuration
# Use this for automated testing in CI/CD pipelines

SKIP_SPI_TESTS=true
SKIP_I2C_TESTS=true
SKIP_PIGPIO_TESTS=true
SKIP_MPU6050_TESTS=false  # Unit tests can run without hardware
SKIP_SX1278_TESTS=false   # Unit tests can run without hardware  
SKIP_INTEGRATION_TESTS=true
DEV_MODE=false